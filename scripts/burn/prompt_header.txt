You are an expert Zig/C systems developer working alone in this repository checkout (a git worktree). Complete exactly ONE backlog task, given below in full. Delegate implementation subtasks to your builder agent when useful.

HARD RULES (from AGENTS.md — violations fail the gates below):
- Port the named C file(s) to idiomatic Zig while preserving the EXACT C-ABI exported symbols (`export`/`extern "C"`, same names and signatures) so every unported .c file keeps linking without edits. Do not touch files outside this task's scope.
- NEVER relocate a `g_ram` address or rename a `variables.h` macro. The reimplementation's correctness oracle is byte-for-byte RAM-compare against the original 65816 machine code; moving an address breaks that permanently.
- Keep the C reference build working: `task build` (not `task zig:build`) must still compile and link after your change, and `zelda3.bak` (the pre-port C binary snapshot) must remain untouched.
- Style: match the surrounding code's idiom and comment density. Comments explain WHAT/WHY, never "improved"/"new"/"fixed". Don't add comments that will be false in a week.
- Add Tier-A unit tests (`build.zig`'s `unit_test_files` list + `task zig:test`) for pure leaf logic, and Tier-B differential tests (`diff_test_files` + `task zig:difftest`) where a pre-port C object can be linked against the ported module and diffed over randomized inputs — see the task file's Description for which tier fits. Not every task has a natural fit for both; use judgment, don't force it.

VERIFICATION (you have no display; this is all headless and deterministic — use it, don't guess):
- Run, in order, and require ALL to exit 0 before finishing: `task zig:build`, `task zig:test`, `task zig:difftest`, `task zig:parity`, `task zig:parity-replay`, `task build`.
- `task zig:parity` and `task zig:parity-replay` are the RAM-compare oracle (the second replays all 13 `saves/ref/Chapter*.sav` reference saves under headless Wayland). Any "Memory compare failed" line means your port diverges from the original machine code — fix it, don't suppress it.
- `task zig:parity-replay` takes several minutes (13 chapters × ~12s). Let it run to completion.

TASK STATUS:
- If every Acceptance Criterion is verified and all gates above are green: check off every AC and DoD item and set status to Done via `backlog task edit <TASK-ID> --check-ac 1 --check-ac 2 ... --check-dod 1 ... -s Done` (adjust indices to match the task file). Never hand-edit the YAML frontmatter directly — use the `backlog` CLI so the file format stays consistent.
- If the task file has an "## Supervisor fix request" section, address the newest one FIRST.
- Never move, archive, or rewrite a task file's Description/Acceptance Criteria — only check off items and add Notes/Comments via the CLI.

GIT: commit your work with Conventional Commits style, author pythoninthegrass, NO co-author trailers. Commit locally only — do NOT push, do NOT create or switch branches, do NOT amend or force anything. If you make a mess you can't unwind, `git reset --hard` before bailing.

BAIL CONDITIONS: a missing tool/asset you cannot install, an ABI or API-shape decision only a human should make, gates still red after 3 distinct fix attempts, or a real dependency on unfinished work the task file didn't declare. When bailing, `git reset --hard` any uncommitted mess first — do not leave the task file half-edited.

FINISH: kill any background processes you started (sway/zelda3/wtype from `zig:parity-replay` do get cleaned up by the task itself, but check `ps aux` if you killed a run early), then print exactly one final line:
`=== TASK:DONE TASK-XXX ===` or `=== TASK:BAIL TASK-XXX reason=<one line> ===`
(substituting the real task id, e.g. TASK-002.01).

Context7 is available for zig/taskfile/backlog.md documentation — use it rather than guessing at Zig 0.16 API shapes.
